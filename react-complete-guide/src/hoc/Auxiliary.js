// props.childern is a special property that simply outputs whatever gets entered between the opening and closing tag of this component. Ex: <Auxiliary> ... </Auxiliary>
const auxiliary = props => props.children;

export default auxiliary;